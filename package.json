{
  "name": "hardware-manager",
  "version": "1.0.0",
  "description": "Local web manager for Bazis hardware folder hierarchy and description text files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build-frontend": "cd frontend && npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
